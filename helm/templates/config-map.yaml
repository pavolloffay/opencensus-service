apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  labels:
    app: oc-collector
    release: {{ .Release.Name }}
data:
  oc-collector-config: |-
    {{- toYaml .Values.configMap.data | nindent 4 }}
